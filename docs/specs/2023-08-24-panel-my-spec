открыл макет и дейтвующую панель, иду по порядку по вкладке dd5
Фиксируй подробно в новом плане тюнинга фронтенда, если по какому-то пункту нужно обсуждение или есть вопрос сразу задавай их до фиксации в плане.
1. В разделе Analysis of published surface  есть кнопка Manifest и lineage  как я понимаю (поправь если я не прав) при анализе поверхности создаются некие файлы чтобы их можно было открыть этой кнопкой. Если так то логично установить следующий порядок: при сохранении базы анализа устанавливаем путь для сохранения базы анализа (по умолчанию это \data\Analysis\ в папке проекта), при создании базы анализа соответствующий файл (файлы) для кнопки Manifest и lineage создаются в новой папке (с именем баps анализа) по этому-же пути.
2. Саму кнопку Manifest и lineage логично перенести ниже как показано на скрине, и при выборе существующей базы анализа в поле Или открыть готовую analysis DB   автоматически привязывать к кнопке Manifest и lineage соответствующую родословную относящуюся к этой базе
3. На экране Shortlist и READY JSON  относительно изначального макета исчез подраздел Phase 2 structural filters . Посмотри в каком состоянии функционал этих фильтров относительно новой структуры базы анализа и определи перечень вопросов по возвращению этого функционала
4. Далее по таблице ниже:
   - верни стиль оформления утвержденный в макете,
   - по составу колонок добавь по каждому ТФ количество найденных плато
   - справочный текст: "Стрелка раскрывает TF, checkbox выбирает все TF группы; отдельные TF можно исключить. READY — сколько кандидатов уйдёт в READY JSON, ALL — сколько всего нашёл анализ. 1ORD — базовый одноордерный выбор: он учитывается в ALL, но в READY JSON этим маршрутом не выгружается. Source PnL — диагностика происхождения, не PnL готовой tested strategy.  " убирай он не нужен
   - добавляем в таблицу новую колонку справа в которой справочно выводим период за который доступны данные по паре-стороне-тф в формате день.месяц (без года), например 01.05-08.10
   - сверху над таблицей добавляем 2 поля выбора периода (начало и конец) тестирования стратегий с возможностью ручного редактирования. Справа для удобства 3 кнопки 1 мес, 2 мес, 3мес
5. Новый каталог JSON-стратегий  - тут по идее путь по умолчанию жестко фиксирован, и это по сути временный каталог куда генерируются стратегии для последующего переноса в тестер. Поле выбора пути можно вообще убрать (задаем только в настройках в файле конфига) по умолчанию этот путь \Output\strategies, оставляем только кнопку Generate READY JSON  при нажатии на которую будет выполняться последовательно сначала полная очистка этой папки, потом генерация в нее стратегий
6. Далее экран Tester batch
   - READY JSON batch  с полем выбора тут не нужно, стратегии берутся всегда из пути куда они генерируются в предыдущем экране при нажатии на кнопку Generate READY JSON
   - Каталог HTML-отчётов - поле указания каталога для хранения ХТМЛ отчетов тоже не нужно, фиксируем что эти отчеты лежат в папке тестера по относительному пути   \tester\report\my_test (при этом абсолютный путь к папке тестера у нас должен быть прописан в конфиг файле.
   - При нажатии на кнопку Проверить и запустить должны быть выполнены следующие действия a) папка с тестами \tester\report\my_test должна быть автоматически очищена от старых отчетов; b) папка стратегий бота по относительному пути \settings_strategy очищается от существующих в ней стратегий; с) Стратегии из ременного каталога куда они были сгенерированы по кнопке Generate READY JSON переносятся в папку тестера по относительному пути \settings_strategy; d) заполнить датами начала и конца файл config_tester.json и сохранить  в корневой папке бота; e)запустить процесс бота и далее активировать существующий скрипт нажатий на тест для тестирования каждой стратегии (существующий бекенд функционал для этого должен быть); f) по завершении тестирования остановить процесс бота;
   - Так-же для отображения процесса тестирования под прогресс баром выводим статусные сообщения "отправлено, в работе, результат, проверено, повторы" прямо в процессе выполнения тестов (этот функционал тоже был уже ранее реализован в бекенде)
7. Экран Inbox → Performance DB
   - путь для performanceDB по умолчанию \data\performanceDB, путь не выбираем, он прописан в конфиг файле.
   - Имя Performance базы формируется автоматом производно от состава пар в батче (-регекс USDT) batch и диапазона теста; существующая DB не перезаписывается.
   - Галку "Удалить HTML-отчёты после успешного import" возвращай к нормальному стилю как в макете, привязывай функционал так чтобы после нажатия на "Импортировать в Performance DB  "  и формирования базы без ошибок при отмеченном чекбоксе отчеты удалялись из папки \tester\report\my_test бота если у нас впридачу к базе предусмотрен файд аудита кладем его автоматически в папку с именем равным именю базы с возможностью открытия по кнопке Показать аудит на этом же экране (ранее такая кнопка была предусмотрена и называлась "Открыть audit "
8. Экран DD5 (или добавит на экран Экран Inbox → Performance DB  а отдельный экран DD5 тогда удалить как ненужный)
   - путь по умолчанию для публикации worlbook \data\workbooks? по этому пути для нового worlbook создаем новую папку с именем перформанс базы
   - Добавляем на этот экран поле для выбора перформанс базы для генерации ВВ5 воркбука, при совпадении имен старый воркбук замещается новым в той-же папке
   - выбранная база и путь для открытия воркбука заполняются автоматически после создания новой перформанс базы
   - на этот же экран возвращаем кнопку Открыть workbook   для открытия воркбука выбранной базы
   - на макете была кнопка Экспорт final shortlist , верно ли я понимаю что такой лист уже и так будет в воркбуке, если так отдельная кнопка не нужна, если нет расскажи что тут должно было быть